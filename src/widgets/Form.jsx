import React from 'react';
import { Input } from 'antd';

Form.propTypes = {

};

function Form( props ) {
  return (
    <div>
      <Input placeholder="Basic usage" />
    </div>
  );
}

export default Form;
